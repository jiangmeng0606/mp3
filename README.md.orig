# MP3 — Music Metadata Batch Processor (Refactored)

本仓库用于批量管理和处理 MP3 文件的音频元数据（ID3 标签）、专辑封面和歌词嵌入。在 `codex` 分支下进行了代码可读性优化与文档增强，功能保持完全兼容。

## 快速上手

**依赖要求：**
- Java 8+
- Maven 3.x（或直接使用随仓库附带的 `mvnw` 脚本）

**编译：**
```bash
./mvnw clean package -DskipTests
```

**运行：**
```bash
java -jar mp3-1.0-SNAPSHOT.jar <command> <input-dir>
# 或使用脚本
bin/mp3 ls ~/Music/mp3
bin/mp3 convert ~/Music/mp3
```

## `codex` 分支变更点
- 新增详细 README，包含背景、用法、扩展建议和安全提醒
- 下一步将按需提交自动格式重构、注释补全等非功能性代码改进

## 支持命令
- `ls` — 列出 MP3 文件及音频/标签详情
- `convert` — 批量修改 MP3（修改标签、加封面、加歌词）

**示例：**
```bash
# 列出目录下所有 mp3
bin/mp3 ls ~/Music/mp3
# 批量元数据标准化
bin/mp3 convert ~/Music/mp3
```

## 目录结构与核心文件

- `src/main/scala/com/haizhi/mp3/` — 主要源码（Main.scala, Mp3Utils.scala）
- `pom.xml` — Maven 构建配置
- `bin/` — Linux/Windows 脚本
- `conf/` — 配置样例
- `mp3-1.0-SNAPSHOT.jar` — 编译后可直接运行的 fat-jar

## 安全与备份建议
- `convert` 命令会原地修改 MP3 文件，建议在大批量操作前自行备份原始数据。
- 重要：脚本有自动备份机制（临时文件写入再覆盖），但如需回滚可将 README.md.orig 恢复为旧 README。

## 贡献指南
- 欢迎反馈和 PR。
- 本分支主要提升文档和自动化重构假设场景下的代码风格（不影响业务逻辑）。
